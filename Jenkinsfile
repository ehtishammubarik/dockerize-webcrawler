pipeline {
    agent any 
    stages {
        stage('Build') { 
            steps {
                sh 'npm -v'
            }
        }
        stage('Test') { 
            steps {
                sh 'npm -v'
            }
        }
    }
}
